# ============================================================
#  Jira Auto-Assign Configuration
#  Fill in your values below before running the application.
# ============================================================

# --- Jira Connection ---
# Your Jira Cloud base URL (no trailing slash)
jira.url=https://lla.atlassian.net
# The email address you use to log into Jira
jira.email=gowtham.krishna@libertypr.com
# API token from: https://id.atlassian.com/manage-profile/security/api-tokens
jira.api-token=ATATT3xFfGF0TMoazubaS8wBJyaIbSZ_Wp_pbBu0ACW2ZDycsmvmV1-UzrHyFaCBDw3JMYL1xK2Y8zKN2Kw_b93zcS-lt68joNj80Y_b13uCfxbnKFo-0oGfujaH_5C60NIHGljqcokYnSzGWSlaWeLRZK4Su0Uf3IpuHAffsrwoSgi6DqUyWWc=394FD5C9
# The Jira project key (e.g. PROJ, DEV, BACKEND)
jira.project-key=SAC

# --- Round-Robin Assignees ---
# Comma-separated list of team member emails.
# Tickets are assigned to each person in order, cycling back to the start.
jira.assignees=gowtham.krishna@libertypr.com

# --- Custom JQL (recommended) ---
# Write any JQL query directly. When set, this OVERRIDES all filters below.
# Leave empty to use the individual filters instead.
# Example: project = PROJ AND sprint in openSprints() AND assignee is EMPTY AND priority = High
jira.custom-jql=project = SAC AND status in ("In Progress","Waiting for support", "Escalated") and "Reporting Area[Dropdown]" = "Order Fallout"

# --- Individual Ticket Filters (used only if custom-jql is empty) ---
# Only pick tickets with these statuses. Leave empty to include all statuses.
jira.target-statuses=Waiting for support
# Only pick these issue types (e.g. Bug,Story,Task). Leave empty for all types.
jira.target-issue-types=
# Only pick tickets with these labels. Leave empty for all labels.
jira.target-labels=

# Only assign tickets that currently have no assignee (recommended: true)
jira.only-unassigned=true

# --- Dry Run ---
# true  = log what would be assigned but make NO changes in Jira (safe to test)
# false = actually assign tickets in Jira
jira.dry-run=false

# --- Scheduler ---
# Cron expression controlling how often the assignment runs automatically.
# Format: second minute hour day-of-month month day-of-week
# Examples:
#   Every 30 min  : 0 */30 * * * *
#   Every hour    : 0 0 * * * *
#   Daily at 9 AM : 0 0 9 * * *
jira.schedule.cron=0 */30 * * * *
